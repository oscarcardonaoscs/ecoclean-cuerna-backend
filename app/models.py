from app.database import Base
from app.models import Producto, Seccion  # Importa tus modelos

# Aquí puedes mantener la base de datos configurada
# models.Base.metadata.create_all(bind=engine) sigue en main.py
